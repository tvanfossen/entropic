Unstage files from the staging area.

**Parameters:**
- `path`: File to unstage (optional, defaults to all staged files)

**Examples:**
- Unstage all: `{}`
- Unstage specific file: `{"path": "src/main.py"}`
