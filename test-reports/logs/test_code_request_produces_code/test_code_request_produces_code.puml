@startuml
skinparam backgroundColor #FEFEFE
skinparam noteBorderColor #999999
skinparam noteBackgroundColor #FFFFEE
title test_code_request_produces_code\n<size:11>[PASSED] < 262s</size>

participant "Test" as T
participant "Application" as App
participant "Engine" as Eng
participant "Orchestrator" as Orch
participant "Model" as Mdl
participant "MCP Tools" as MCP

T -> App: process_message
note right of T
  **Prompt:**
  Write a Python function to check if a number is even
end note
App -> Orch: route
note right: **CODE** tier
== Turn 1 ==
Orch -> Mdl: generate_stream
note right of Mdl
  **Model Output:**
  Here's a Python function to check if a number is even:
  
  ```python
  def is_even(num):
      return num % 2 == 0
  ```
  
  **Usage example:**
  ```python
  print(is_even(4))   # Output: True
  print(is_even(5))   # Output: False
  print(is_even(-6))  # Output: True
  print(is_even(0))   # Output: True
  ```
  
  The function works for:
  - Positive numbers
  - Negative numbers
  - Zero
  - Both integers and floating-point numbers (e.g., 3.0 would return False)
  
  Would you like me to add any additional features or handle specific edge cases?
end note
note over T #palegreen
  **PASSED** (< 262s)
end note

@enduml