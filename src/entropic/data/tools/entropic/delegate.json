{
  "name": "delegate",
  "description": "Delegate a task to a different identity tier.\n\nUse this to assign work to a tier whose specialty matches the task. Provide a clear task description so the receiving tier has full context.\n\nThe available tiers are listed in the `target` enum. Choose based on the tier whose specialty matches the work that needs to be done.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "target": {
        "type": "string",
        "enum": [],
        "description": "Target identity tier to delegate to"
      },
      "task": {
        "type": "string",
        "description": "Natural language description of the task to delegate"
      },
      "max_turns": {
        "type": "integer",
        "minimum": 1,
        "maximum": 30,
        "description": "Maximum number of turns the delegate can use (optional)"
      }
    },
    "required": ["target", "task"]
  }
}
