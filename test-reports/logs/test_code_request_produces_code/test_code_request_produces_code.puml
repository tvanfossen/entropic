@startuml
skinparam backgroundColor #FEFEFE
skinparam noteBorderColor #999999
skinparam noteBackgroundColor #FFFFEE
title test_code_request_produces_code\n<size:11>[PASSED] < 262s</size>

participant "Test" as T
participant "Application" as App
participant "Engine" as Eng
participant "Orchestrator" as Orch
participant "Model" as Mdl
participant "MCP Tools" as MCP

T -> App: process_message
note right of T
  **Prompt:**
  Write a Python function to check if a number is even
end note
App -> Orch: route
note right: **LEAD** tier
== Turn 1 ==
Orch -> Mdl: generate_stream
note right of Mdl
  **Model Output:**
  I'll write a Python function to check if a number is even:
end note
Mdl --> Eng: tool_call(filesystem.write_file)
Eng -> MCP: filesystem.write_file
note right of MCP
  path: check_even.py
  content: def is_even(n):
    """Check if a number is even.
    
    Args:
        n (int): The number to check
        
    Returns:
        bool: True if the number is even, False otherwise
        
    Examp
end note
MCP --> Eng: result
note right of MCP
  **Tool Result:**
  {"success": true, "bytes_written": 587, "path": "check_even.py"}
end note
note right of Eng
  **Follow-up message:**
  &lt;tool_response&gt;
  {"success": true, "bytes_written": 587, "path": "check_even.py"}
  &lt;/tool_response&gt;
  
  Continue. Batch multiple tool calls in one response when possible.
end note
== Turn 2 ==
Orch -> Mdl: generate_stream
note right of Mdl
  **Model Output:**
  Done! I've created `check_even.py` with a Python function that checks if a 
  number is even. The function:
  
  - Takes a single parameter `number` (the integer to check)
  - Returns `True` if the number is even, `False` otherwise
  - Uses the modulo operator (`%`) to perform the check
  
  You can use it like this:
  ```python
  from check_even import is_even
  
  if is_even(4):  # Returns True
      print("It's even!")
  ```
end note
note over T #palegreen
  **PASSED** (< 262s, 1 tool calls)
end note

@enduml