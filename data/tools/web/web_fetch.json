{
  "name": "web_fetch",
  "description": "Fetch a web page and return its content as plain text.\n\nUse this to read documentation, articles, or other web content. HTML is stripped to produce readable text. Content is truncated to stay within context limits.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "URL to fetch"
      },
      "max_length": {
        "type": "integer",
        "description": "Maximum content length in characters (default: 20000)",
        "default": 20000
      }
    },
    "required": ["url"]
  }
}
