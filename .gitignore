# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
ENV/

# IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/

# Type checking
.mypy_cache/

# Local config (don't commit)
.entropic/config.local.yaml
.entropic/*.local.*

# Models (too large)
*.gguf

# Database
*.db
*.sqlite

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# Example runtime artifacts
examples/pychess/.venv/
examples/pychess/.pychess/

# Build artifacts
*.whl

# Clone-at-install build directory (llama-cpp-python)
.build/

# Jupyter
.ipynb_checkpoints/

# Environment files
.env
.env.local
# test-reports/ — runtime path is build/test-reports/ (covered by `build/`
# above). The root-level rule below is a belt-and-suspenders guard in
# case something writes there from cwd; nothing in current code does.
test-reports/

.claude/tmp/

# Git worktrees for delegation isolation
.worktrees/

# Archived legacy files
.old/


# build cache
.cache/

# CTest scratch (CMake redirects this under build/, keep root rule as legacy)
Testing/

# Generated Doxygen output — Doxyfile OUTPUT_DIRECTORY = build/docs
# Legacy paths kept so stale generators or bare `doxygen` from CWD
# don't sneak artifacts into git.
build/docs/
html/
latex/
sqlite3/
api/

# Release tarballs
dist/
