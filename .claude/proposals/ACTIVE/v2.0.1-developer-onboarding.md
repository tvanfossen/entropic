---
version: 2.0.1
title: Developer Onboarding & Examples Porting
status: active
created: 2026-03-19
updated: 2026-04-06
dependencies: [v2.0.0]
---

## Scope

Developer experience and examples validation for the 2.0.0 release.
Three workstreams:

1. **Examples porting** — each existing Python example gets two new
   implementations: a direct C/C++ application and a Python wrapper
   version. These prove the C API and Python wrapper are complete
   enough for real consumers.

2. **Human learning plan** — structured walkthrough of the C++ engine
   internals, ordered by dependency graph (bottom-up).

3. **Documentation refresh** — README, CONTRIBUTING.md, architecture
   diagrams updated for the C++ engine.

### Completed (prior sessions)

- [x] Part 1 (invoke tasks) — `tasks.py` exists with `build`, `test`,
  `clean`, model test runner with retries + results.json. No further
  work needed.

---

## Part 1: Examples Porting

Two existing Python examples target the v1.7.1 Python engine:

| Example | Complexity | Demonstrates |
|---------|-----------|--------------|
| `hello-world/` | Minimal | Single tier, streaming, app context |
| `pychess/` | Full | Multi-tier pipeline, grammar constraints, custom MCP tool, per-tier identities, handoff |

Each gets **two new implementations** that must produce equivalent
behavior:

### hello-world

**C/C++ version** (`examples/hello-world/main.c` or `main.cpp`):
- Link against `librentropic.so` (facade)
- `entropic_create()` → `entropic_configure()` → `entropic_run_streaming()` → `entropic_destroy()`
- Streaming callback prints tokens
- Interactive prompt loop
- Config loaded from same `config.yaml`

**Python wrapper version** (`examples/hello-world/main_wrapper.py`):
- `import entropic` (auto-generated wrapper from `entropic.h`)
- Same API surface as C but Pythonic
- Streaming via callback or iterator
- Proves the wrapper round-trips correctly

### pychess

**C/C++ version** (`examples/pychess/main.cpp`):
- Custom MCP server plugin (`chess_server.so`) exposing `chess.make_move`
- Multi-tier config (thinker + executor)
- Grammar constraints loaded from `.gbnf` files
- Handoff via `entropic.handoff` directive
- Board state managed in C, moves validated via chess logic
- Depends on a C chess library or embedded move validation

**Python wrapper version** (`examples/pychess/main_wrapper.py`):
- `import entropic` wrapper + `python-chess` for board management
- Register `chess.make_move` tool via wrapper API
- Same multi-tier pipeline behavior as the Python original
- Grammar constraints passed through wrapper

### Blockers

| Blocker | Status | Required for |
|---------|--------|-------------|
| C API facade wired (not stubs) | **Done** (v2.0.0, 62 functions) | Both C and Python versions |
| Python wrapper auto-generated | **Done** (69 functions wrapped) | Python wrapper versions |
| MCP plugin loading (`dlopen`) | **Done** (stdio transport) | pychess C version |

All blockers resolved as of v2.0.0.

### Validation

- C/C++ hello-world: `cmake --build` + run → interactive streaming chat
- C/C++ pychess: `cmake --build` + run → play chess, AI responds with
  legal moves via grammar-constrained pipeline
- Python hello-world: `python main_wrapper.py` → identical behavior
- Python pychess: `python main_wrapper.py` → identical behavior
- All four must run against the same model on the same hardware
- Output logged for comparison (token-for-token match not required,
  behavioral equivalence sufficient)

---

## Part 2: Human Learning Plan

Structured walkthrough of the C++ engine, ordered by dependency graph
(bottom-up). Each item is a reading + modification exercise.

### Layer 1: Types & Build System (1-2 hours)

- Read `CMakeLists.txt` top-level + all presets
- Read `entropic_config.h.in` → trace to generated header
- Read `entropic_export.h` → understand symbol visibility
- Read `types/error.h` → trace through `entropic_error_name()`
- Read `types/config.h` → all config structs, defaults
- **Exercise:** Add a new error code, verify it round-trips through tests

### Layer 2: Config & Prompts (2-3 hours)

- Read `config/yaml_util.h` → extraction pattern
- Read `config/loader.cpp` → layered merge logic
- Read `config/validate.cpp` → cross-field validation
- Read `prompts/manager.cpp` → frontmatter parsing
- Read `prompts/classification.cpp` → router prompt builder
- **Exercise:** Add a new config field end-to-end (struct → parse → validate → test)

### Layer 3: Inference (3-4 hours)

- Read `i_inference_backend.h` → C boundary contract
- Read `inference/backend.h` → concrete base (80% logic)
- Read `inference/llama_cpp_backend.*` → 20% override
- Read `inference/adapters/adapter_base.h` → chat formatting
- Read `inference/adapters/qwen35.*` → XML tool calls
- Read `inference/orchestrator.h` → tier routing, VRAM lifecycle
- **Exercise:** Add a metric to the inference backend, expose via C API

### Layer 4: Engine Core (3-4 hours)

- Read `core/engine.h` → agentic loop, state machine
- Read `core/response_generator.*` → system prompt assembly
- Read `core/context.*` → message management, compaction trigger
- Read `core/directives.*` → tool result → engine action
- **Exercise:** Add a new directive type end-to-end

### Layer 5: MCP & Tools (2-3 hours)

- Read `i_mcp_server.h` → plugin C boundary
- Read `mcp/server_base.h` → 80% dispatch logic
- Read one server implementation (filesystem or bash)
- Read permission manager → default-deny enforcement
- **Exercise:** Write a minimal custom MCP server plugin .so

### Layer 6: Integration (2-3 hours)

- Read `entropic.h` → full C API surface
- Read facade implementation → how C API maps to internals
- Read Python wrapper → auto-generated bindings
- **Exercise:** Write a C program that uses the full lifecycle

---

## Part 3: Documentation Refresh

- Update README for C++ build instructions
- Write CONTRIBUTING.md with build/test/lint workflow
- Verify Doxygen output is navigable and complete
- Add architecture diagrams to docs/ (PlantUML sequence diagrams)

---

## Validation Criteria

1. `inv build` works from clean checkout
2. `inv test` runs all tests
3. All 4 example implementations run and produce correct behavior
4. Each learning exercise has a working solution
5. README and CONTRIBUTING.md are current

## Implementation Log

### 2026-04-10 (session 2)
- [x] **Facade purity — subsystem extraction** (~80 lines moved out of facade):
  - `prompts::resolve_tier_identity()` — extracted identity loading from `facade_resolve_tier`
  - `TierConfig::get_param()` — extracted policy mapping from `facade_get_tier_param`
  - `ModelsConfig::find_tier_by_path()` — extracted config lookup from `find_tier_by_model_path`
  - `log::setup_session()` — extracted log file setup from `entropic_configure_dir`
  - `SessionLogger` class (types.so) — extracted FILE* model log from `entropic_run_streaming`
  - `facade_json::serialize_adapter_info/list()` — extracted JSON from adapter APIs
  - `wire_engine_interfaces()` + `init_persistence()` — split `configure_common` (was SLOC 70)
  - `stream_chunk_cb()` + `StreamCtx` struct — extracted from `entropic_run_streaming` (was SLOC 55, returns 5)
- [x] **Wrapper purity — cancel thread removed**: polling in callback replaces `threading.Thread`
- [x] **All facade knots violations resolved** (configure_common, entropic_run_streaming)
- [x] v2.0.5 CI/CD proposal created
- **Pre-existing knots violations** (not introduced this session): engine.cpp, orchestrator.cpp, tool_executor.cpp, llama_cpp_backend.cpp
- [x] **Facade fully purified** — all business logic moved to engine:
  - ConversationState → engine (run_turn, run_streaming, clear, count, get_messages)
  - StreamThinkFilter → core.so (moved from inference.so) + UTF-8 codepoint alignment
  - Streaming lifecycle → engine (run_streaming owns filter, cancel, session logger)
  - Tier resolution → engine (set_tier_info, set_handoff_rules, internal resolution)
  - Directive hooks → engine (build_directive_hooks)
  - ToolExecutorHooks struct → engine_types.h (was in mcp/tool_executor.h)
- [x] **Wrapper fully purified** — UTF-8 buffer removed (engine guarantees aligned chunks)
- [x] **User validated** hello-world + pychess wrappers work
- [x] v2.0.7 OpenAI-compat REST server proposal created
- **v2.0.1 COMPLETE** — ready for STAGED

### 2026-04-10
- [x] **Documentation comprehensive review** — 3-agent deep verification of README, getting-started, library-consumer-guide
- [x] Fixes: C++17→C++20, config resolution order (4→5 layers), removed unverified throughput numbers
- [x] Pychess `entropic.handoff` → `entropic.delegate` (tool name mismatch)
- [x] **cancel_flag wired** — polled in stream callback, calls engine->interrupt()
- [x] **SqliteStorageBackend wired** — auto-opens in project dir during configure_dir
- [x] **Python wrapper UTF-8 fix** — buffer incomplete multi-byte sequences across token boundaries
- [x] **pyproject.toml cleanup** — license LGPL-3.0, removed stale Python deps (httpx, pyyaml)
- [x] Stale docs moved to `docs/stale/` (8 Python-era files)
- [x] v2.0.3 MCP proposal expanded: bridge + .mcp.json discovery + headless example
- [x] v2.0.4 explorer proposal created: doxygen SQLite + guided architecture tour
- **Pending user validation:** hello-world wrapper (#49), pychess wrapper (#50)

### 2026-04-09
- [x] **ToolExecutor properly wired** — replaces facade_tool_executor with real ToolExecutor class
- [x] **DirectiveProcessor connected** — ToolExecutorHooks bridge to engine directive handlers
- [x] **core.so uses nlohmann/json** — removed empty-name ToolCall hack, proper JSON parsing
- [x] **Directive extraction in ToolExecutor** — extracts typed directives from ServerResponse JSON
- [x] **should_stop_batch** — checks COMPLETE/pending_delegation states
- [x] **process_tool_results preserves terminal states** — no longer overwrites COMPLETE with EXECUTING
- [x] **Tier routing via params_json** — InferenceInterface carries tier name, multi-tier works
- [x] **Grammar registration** — chess_thinker/chess_executor registered explicitly after configure
- [x] **init_builtins on ServerManager** — registers built-in MCP servers from config flags
- [x] **enable_entropic config flag** — new MCPConfig field for entropic internal server
- [x] **Tier resolution callbacks** — facade_resolve_tier, facade_tier_exists, facade_get_handoff_targets, facade_get_tier_param
- [x] **Auto-complete after delegation** — tiers without explicit_completion auto-complete on successful delegation
- [x] **Pychess executor grammar** — chess.make_move + entropic.complete in sequence
- [x] **Pychess permissions** — auto_approve: true driven by config, not hardcoded
- [x] **Model path validation** — orchestrator checks file exists at configure, clear error + download hint

### 2026-04-08
- [x] **Facade wiring: AgentEngine** — entropic_run/run_streaming now go through engine->run() with full prompt assembly, context, tool loop
- [x] **Conversation state** — persistent multi-turn history, entropic_context_clear/get/count C API
- [x] **entropic_configure_dir** — new C API for layered config (consumer defaults → global → project local → env)
- [x] **System prompt assembly** — constitution + app_context + identity loaded and cached, prompts::assemble() in prompts subsystem
- [x] **Session logging** — session.log (spdlog file sink, all engine logs), session_model.log (raw streaming user/assistant)
- [x] **Think-block filtering** — StreamThinkFilter in inference layer, strips `<think>` from consumer stream
- [x] **ggml log redirect** — silenced by default, optional llama_ggml.log via ggml_logging config
- [x] **CUDA graphs disabled** — ENTROPIC_CUDA_GRAPHS=OFF default (no benefit for autoregressive chat)
- [x] **Prompt cache crash fix** — chunked decode in restore_cached_prefix (was exceeding n_batch)
- [x] **Security fixes** — getenv NULL guard, FILE* leak on exception, alloc_cstr replacing strdup
- [x] **Facade refactor (Steps 0-7)** — ~345 lines extracted to proper subsystems:
  - prompts::assemble() (Step 1)
  - config::load_layered() (Step 2)
  - InferenceInterfaceFactory (Step 3)
  - StreamThinkFilter (Step 4)
  - ConversationState + json_serializers (Step 5)
  - BundledModels::auto_discover_and_load() (Step 6)
  - ggml log → orchestrator (Step 7)
- [x] **Tier routing** — InferenceInterface now carries tier via params_json, multi-tier pipelines work
- [x] **Model path validation** — orchestrator checks file exists at configure, clear error + download hint
- [x] **App context improvements** — model vs engine identity clarity, jailbreak mitigations, refusal brevity
- [x] **inv example task** — build + run examples with proper lib paths, CUDA/CPU preset selection
- [x] **Consumer defaults** — default_config.yaml per example (loaded as Layer 0 before global config)
- [x] **Bundled assets** — data/bundled_models.yaml + data/prompts/ copied to C engine data dir
- [x] **Adversarial analysis** — 6-agent facade audit, jailbreak resistance review (10-turn escalation, A- grade)
- [x] **Model grading** — 6-turn session graded by independent agents (A- to D+ range)
- **v2.0.3 MCP bridge** — proposal created in BACKLOG (entropic mcp-bridge CLI subcommand)
- **Pychess status:** grammar registration wired, tier routing wired, needs end-to-end retest

### 2026-04-07
- [x] examples/hello-world/main.c — C API version (create → configure_from_file → streaming loop → destroy)
- [x] examples/hello-world/main_wrapper.py — Python wrapper version (EntropicEngine + run_streaming)
- [x] examples/hello-world/CMakeLists.txt — standalone build linking librentropic
- [x] examples/pychess/main.cpp — C++ version (multi-tier, external MCP server, grammar-constrained, streaming)
- [x] examples/pychess/main_wrapper.py — Python wrapper version (EntropicEngine + python-chess + external MCP)
- [x] examples/pychess/CMakeLists.txt — standalone C++ build linking librentropic
- [x] examples/pychess/chess_server.py — enhanced with JSON-RPC 2.0 stdio MCP protocol (stateless, acknowledges moves)
- [x] README.md — updated with C++ build instructions (invoke commands, link instructions, expanded C example)
- [x] CONTRIBUTING.md — new: clone, build presets, pre-commit, test workflow, code style, branch/proposal workflow
- [x] License updated in README: LGPL-3.0 (v2.0.0+)
- [x] All pre-commit gates pass: knots, flake8, doxygen-guard, build, 673 tests, coverage
- **Architecture note:** C++ pychess uses simple 8x8 board (no C chess library) — MCP server is stateless.
  AI move parsed from streaming output. Full board validation in wrapper version via python-chess.
- **Pending user validation:** Examples need runtime testing with model loaded. Original Python examples
  (main.py, engine.py) stay until user confirms replacements.

### 2026-04-06
- [x] Updated proposal — removed completed invoke task section,
  added examples porting as Part 1 with dual-implementation
  requirement (C/C++ + Python wrapper per example)
- [x] Identified blockers: C API facade stubs, Python wrapper
  not yet generated
