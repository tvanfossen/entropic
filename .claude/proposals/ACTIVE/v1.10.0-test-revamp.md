---
version: 1.10.0
title: Test Architecture Revamp
status: active
created: 2026-03-18
updated: 2026-03-18
dependencies: [v1.9.15]
---

## Scope

Reorganize the test infrastructure into a coherent, maintainable
architecture. After v1.10.0, the project has:

- Conventional test mapping: test file paths mirror source paths
- Model tests and benchmarks merged into a single GPU-required suite
- Manual integration tests (test1-test6 style) documented but unchanged

This version produces NO new engine code. It reorganizes the test
infrastructure built across v1.8.0 through v1.9.14.

**Deferred to v1.10.2:** doxygen-guard review/implementation, `@req`
traceability, documentation review, impact-based test selection
(`scripts/select_tests.py`). All run full test suite until then.

**What does NOT change:**
- No new public API surface
- No interface header modifications
- No changes to engine behavior
- No new library `.so` files
- Pre-commit still runs unit tests (fast, CPU, no GPU)
- Model tests remain developer-run with checked-in reports
- Manual integration tests remain manual
- CI runs full test suite on all builds

---

## Implementation Plan

### Phase 1: Conventional Test Mapping

Restructure the test tree so test paths mirror source paths. The mapping
rule is mechanical: `src/{lib}/{file}.cpp` → `tests/unit/{lib}/{file}_test.cpp`.

#### Target Structure

```
tests/
├── unit/
│   ├── core/
│   │   ├── agent_engine_test.cpp         mirrors src/core/agent_engine.cpp
│   │   ├── context_manager_test.cpp      mirrors src/core/context_manager.cpp
│   │   ├── response_generator_test.cpp   mirrors src/core/response_generator.cpp
│   │   ├── directive_processor_test.cpp  mirrors src/core/directive_processor.cpp
│   │   └── delegation_manager_test.cpp   mirrors src/core/delegation_manager.cpp
│   ├── config/
│   │   ├── config_loader_test.cpp
│   │   ├── config_validator_test.cpp
│   │   └── prompt_manager_test.cpp
│   ├── inference/
│   │   ├── llama_cpp_backend_test.cpp
│   │   ├── chat_adapter_test.cpp
│   │   ├── grammar_registry_test.cpp
│   │   └── prompt_cache_test.cpp
│   ├── mcp/
│   │   ├── mcp_server_base_test.cpp
│   │   ├── tool_registry_test.cpp
│   │   ├── tool_executor_test.cpp
│   │   └── permission_manager_test.cpp
│   ├── identity/
│   │   ├── identity_manager_test.cpp
│   │   └── todo_tool_test.cpp
│   ├── hooks/
│   │   └── hook_registry_test.cpp
│   ├── storage/
│   │   └── session_store_test.cpp
│   ├── types/
│   │   └── error_types_test.cpp
│   └── api/
│       └── entropic_api_test.cpp
├── integration/
│   ├── engine_loop_test.cpp
│   ├── delegation_pipeline_test.cpp
│   ├── tool_calling_loop_test.cpp
│   ├── external_mcp_test.cpp
│   └── session_persistence_test.cpp
├── regression/                           (from v1.10.1 — empty placeholder)
├── stress/                               (unchanged from v1.9.14)
├── edge/                                 (unchanged from v1.9.14)
├── concurrency/                          (unchanged from v1.9.14)
├── model/                                (merged suite)
│   ├── CMakeLists.txt
│   ├── model_inference_test.cpp
│   ├── model_routing_test.cpp
│   ├── model_identity_test.cpp
│   ├── bench_inference.cpp
│   ├── bench_engine_overhead.cpp
│   └── bench_memory_profile.cpp
├── manual/
│   └── README.md
├── valgrind.supp
└── CMakeLists.txt
```

The move is purely structural. No test logic changes.

---

### Phase 2: Requirement Traceability via doxygen-guard

Replace custom `@test` annotation tooling with doxygen-guard's `@req` tag
system. Requirements are defined in `docs/requirements.yaml` (created in
this session). Every public function links to one or more requirements.

#### Annotation Format

```cpp
/**
 * @brief Generate a response from the loaded model.
 * @version 1.8.2
 * @req REQ-INFER-003
 * @req REQ-INFER-005
 * @req REQ-LOOP-006
 */
ENTROPIC_EXPORT entropic_error_t entropic_inference_generate(
    entropic_inference_backend_t backend,
    const char* messages_json,
    const char* params_json,
    char** result_json);
```

**Traceability chain:**
```
Code (@req REQ-INFER-003) → docs/requirements.yaml (REQ-INFER-003)
  → acceptance_criteria → test suite mapping
```

#### doxygen-guard Configuration

`.doxygen-guard.yaml`:
```yaml
validate:
  presence:
    require_doxygen: true
    skip_forward_declarations: true
  version:
    require_present: true
    require_increment_on_change: true
  tags:
    req:
      pattern: "^REQ-[A-Z]+-\\d{3}$"
  exclude:
    - "^extern/"

impact:
  requirements:
    file: docs/requirements.yaml
    format: yaml
  test_mapping:
    - match: "REQ-LOOP-.*"
      suite: "Engine Loop"
      command: "ctest --test-dir build --tests-regex 'agent_engine|context_manager|directive'"
    - match: "REQ-DELEG-.*"
      suite: "Delegation"
      command: "ctest --test-dir build --tests-regex 'delegation'"
    - match: "REQ-TOOL-.*"
      suite: "Tool System"
      command: "ctest --test-dir build --tests-regex 'tool|mcp|permission'"
    - match: "REQ-IDEN-.*"
      suite: "Identity"
      command: "ctest --test-dir build --tests-regex 'identity|prompt'"
    - match: "REQ-INFER-.*"
      suite: "Inference"
      command: "ctest --test-dir build --tests-regex 'inference|backend|adapter'"
    - match: "REQ-CFG-.*"
      suite: "Config"
      command: "ctest --test-dir build --tests-regex 'config|loader|validator'"
    - match: "REQ-STOR-.*"
      suite: "Storage"
      command: "ctest --test-dir build --tests-regex 'storage|session'"
    - match: ".*"
      suite: "Full Regression"
      command: "ctest --test-dir build"
  output:
    format: markdown
```

#### Impact-Driven Test Selection

When a function with `@req REQ-INFER-003` changes, doxygen-guard's impact
analysis reports that REQ-INFER-003 is affected. The test_mapping in the
config maps that to the "Inference" suite. `scripts/select_tests.py`
consumes this to select the right ctest regex.

This replaces the custom `@test` → `test_map.json` pipeline with
doxygen-guard's built-in requirement traceability.

---

### Phase 3: Git Diff Test Selection

`scripts/select_tests.py` — given changed files, output minimal test set.

#### Selection Algorithm

Two input sources, merged:

1. **Mirror path mapping** (structural): `src/{lib}/{file}.cpp` →
   `tests/unit/{lib}/{file}_test.cpp`
2. **doxygen-guard impact** (semantic): changed function has `@req REQ-xxx`
   → requirements.yaml → test_mapping → ctest regex

The script runs `doxygen-guard impact --staged` (or `--range`) to get
affected requirements, then maps to test suites. Mirror paths provide
fallback when `@req` tags are incomplete.

#### Selection Rules

| Changed file pattern | Selected tests |
|---|---|
| `src/{lib}/{file}.cpp` | Mirror + doxygen-guard impact |
| `include/entropic/{header}.h` | All tests including that header + impact |
| `include/entropic/interfaces/i_*.h` | ALL tests (interface change = full) |
| `tests/unit/**` | The changed test file itself |
| `CMakeLists.txt` | ALL tests |
| `entropic_config.h.in` | ALL tests |
| `docs/**`, `*.md` | No tests |

#### Fallback Behavior

If doxygen-guard is not installed or `@req` tags are sparse, the script
falls back to mirror-path-only selection. Log a warning. Never fail due
to missing doxygen-guard — degrade gracefully.

---

### Phase 4: Model Test + Benchmark Merge

Merge model quality tests and performance benchmarks into `tests/model/`.
Both require GPU, both run at minor version bumps, both produce checked-in
reports. One suite, one gate.

`ENTROPIC_BUILD_MODEL_TESTS=OFF` by default. Developer enables for
minor version validation.

Report format: unified JSON in `test-reports/model/` with quality +
benchmark results per version.

---

### Phase 5: CI Pipeline Integration

PR builds run selected tests only. Full suite on merge/nightly.

Safety triggers forcing full runs:
- Interface header changes
- CMake/build system changes
- Test selector script changes
- Merge to main or develop

---

### Phase 6: Manual Integration Test Documentation

`tests/manual/README.md` — documents test1-test6 patterns, maps each
to automation candidates for v1.10.1 regression suite.

---

## Files Created/Modified

### New

| File | Purpose |
|---|---|
| `scripts/select_tests.py` | Git diff test selection |
| `.doxygen-guard.yaml` | doxygen-guard configuration for entropic |
| `tests/manual/README.md` | Manual test documentation |
| `tests/model/CMakeLists.txt` | Merged model test suite |
| `test-reports/model/` | Unified report directory |

### Modified

| File | Change |
|---|---|
| `tests/unit/**` | Restructured to mirror source paths |
| `tests/CMakeLists.txt` | Updated paths |
| `include/entropic/*.h` | `@req` annotations added to public functions |
| `.github/workflows/build.yml` | Test selection integration |
| `.pre-commit-config.yaml` | doxygen-guard hook (added in task 5 of this session) |

### Removed

| File | Reason |
|---|---|
| `scripts/parse_test_annotations.py` | Replaced by doxygen-guard |
| `tests/test_map.json` | Replaced by doxygen-guard impact analysis |

---

## Validation Criteria

1. Every `src/{lib}/{file}.cpp` has a `tests/unit/{lib}/{file}_test.cpp`
2. All tests pass after reorganization (zero regressions)
3. doxygen-guard validates all public headers without errors
4. `@req` tags present on all functions in `entropic.h` (incremental — WARNING not ERROR)
5. `select_tests.py` correctly selects tests for 10 representative diffs
6. `tests/model/` contains all model tests and benchmarks
7. CI PR pipeline uses test selection
8. CI merge pipeline runs full suite
9. doxygen-guard pre-commit hook passes on clean repo
10. `tests/manual/README.md` documents test1-test6

---

## Dependencies

- v1.9.15: complete engine with all tests (input to reorganization)
- doxygen-guard: pre-commit hook for `@req` validation and impact analysis
- docs/requirements.yaml: requirement definitions (created this session)

---

## Unit Tests

No new engine tests. This version reorganizes existing tests and adds
infrastructure tooling. The test of this version IS whether all existing
tests still pass after reorganization.

---

## Implementation Notes (from review)

1. **@req annotation effort is incremental.** Retroactively annotating 50+
   functions is a one-time cost. doxygen-guard tracks unmapped functions.
   The CI check starts as WARNING (v1.10.0), promoted to ERROR in v1.10.1.
   Annotate as you touch files, close gaps systematically.

2. **~25 missing test scenarios from v1.8.x (from 2026-03-20 audit).**
   Test files planned in proposals but never written during implementation:
   - v1.8.1: test_yaml_util.cpp (8 scenarios), test_env_overrides.cpp (4 scenarios)
   - v1.8.3: 3 config integration tests (parse_prompt_cache_config, defaults, disabled)
   - v1.8.4: test_response_generator.cpp (unwritten)
   These should be written as part of the test reorganization, not as
   separate backfill work. The reorganized test structure will place them
   in the correct locations.

---

## Implementation Log

### 2026-04-01
- [x] Phase 1: Renamed all 72 unit test files to `{name}_test.cpp` suffix convention
  - Root-level files moved into `types/`, `config/`, `prompts/`, `api/` subdirs
  - `tests/unit/CMakeLists.txt` updated with all new paths
  - 637/637 tests pass, git mv preserves history
- [x] Phase 2: **DEFERRED** — doxygen-guard undergoing major changes, `@req` tags deferred to v1.10.x
- [x] Phase 3: Created `scripts/select_tests.py` with mirror-path-only test selection
  - Unified `_PREFIX_MAPPINGS` table covers test files, source files, and headers
  - Interface/CMake/config changes trigger full runs
  - doxygen-guard impact integration stubbed (deferred with Phase 2)
- [x] Phase 4: Created `tests/model/CMakeLists.txt` with `ENTROPIC_BUILD_MODEL_TESTS=OFF` gate
  - Merges model quality tests + benchmarks into one directory
  - Source files placeholder — C++ model tests not yet written
  - `ENTROPIC_BUILD_MODEL_TESTS` option added to root CMakeLists.txt
- [x] Phase 5: Updated `.github/workflows/build.yml`
  - PR builds use `select_tests.py` for selective testing
  - Push to main/develop runs full suite
  - Added `fetch-depth: 0` and `setup-python` for diff script
- [x] Phase 6: Created `tests/manual/README.md`
  - Documents test1-test6 patterns with requirement coverage
  - Maps each to automation candidates for v1.10.1 regression suite
- **Decision:** Phase 2 (@req traceability) deferred to v1.10.2 proposal
  dependent on doxygen-guard stabilization.
- **Decision:** Phase 3 (select_tests.py) and Phase 5 (CI test selection)
  removed — test selection depends on doxygen-guard impact analysis, moved
  to v1.10.2. Full test suite runs on all builds until then.
- **Files changed:** tests/model/CMakeLists.txt, tests/manual/README.md,
  tests/CMakeLists.txt, tests/unit/CMakeLists.txt, CMakeLists.txt,
  .github/workflows/build.yml (reverted to full runs), 72 test file renames
