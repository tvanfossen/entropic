Get git diff showing changes.

**Parameters:**
- `staged`: If true, show staged changes only (default: false)
- `path`: Optional file path to diff

**Examples:**
- All unstaged changes: `{}`
- Staged changes only: `{"staged": true}`
- Specific file: `{"path": "src/main.py"}`
