---
version: 2.1.9
status: staged
created: 2026-05-13
updated: 2026-05-13
dependencies: [v2.1.8]
source:
  - https://github.com/tvanfossen/entropic/issues/44
  - https://github.com/tvanfossen/entropic/issues/45
  - https://github.com/tvanfossen/entropic/issues/46
  - https://github.com/tvanfossen/entropic/issues/47
  - https://github.com/tvanfossen/entropic/issues/17
session: Session 2 of 2.2.0 release plan
---

# v2.1.9 — Model registry expansion + 3 new adapter classes

## Session prompt

This patch broadens the bundled model registry from 2 entries (Qwen3.5-A3B primary, Qwen3-0.6B router) to 11, and adds three new chat adapter classes to handle the new families. Each model gets its own registry key using a consistent `<family>_<size_or_variant>` naming convention; tier role assignment is a separate config concern, so the same key can be swapped into `primary`, `mid`, `draft`, `router`, etc.

After this patch:
- 9 new model registry entries are available for tier configs to reference.
- Three new adapter classes exist: `Qwen36Adapter`, `Gemma4Adapter`, `Nemotron3Adapter`.
- Each new adapter has unit tests verifying render + tool-call parse correctness, and at least one model test exercising real inference against a bundled GGUF in the family.
- Tier swapping is purely a config edit — no code changes needed to switch primary from Qwen3.5-A3B to Qwen3.6-A3B, or to use Gemma 4 26B-A4B as a primary alternate.

## Issues covered

| Issue | Subject |
|---|---|
| gh#44 | Expand `bundled_models.yaml` — Qwen3.5/3.6, Gemma 4, Nemotron 3 |
| gh#45 | `Qwen36Adapter` — distinct adapter class for Qwen3.6 family |
| gh#46 | `Gemma4Adapter` — covers Gemma 4 26B-A4B / E4B / E2B |
| gh#47 | `Nemotron3Adapter` — Nemotron-3-Nano-4B (arch-verify gated) |
| gh#17 | (umbrella — partial ratchet: 3 families land here; remaining families deferred to 2.3.x) |

## Design decisions already made (do not re-litigate)

- **No version lumping.** Each model generation gets its own adapter class. `Qwen35Adapter` stays as-is for Qwen3.5; `Qwen36Adapter` is a new distinct class for Qwen3.6. If template logic happens to overlap, a `QwenBaseAdapter` intermediate can be extracted **as a result of implementation**, not as a precondition.
- **Naming convention.** `<family>_<size_or_variant>`, lowercase, underscores. Examples: `qwen3_5_9b`, `qwen3_6_a3b`, `gemma4_e2b`, `nemotron3_nano_4b`. Documented in a comment block at the top of `data/bundled_models.yaml`.
- **Adapter key matches family.** Config field `adapter: "qwen35"` / `"qwen36"` / `"gemma4"` / `"nemotron3"`. No cross-family aliases — keys do not silently accept other generations.
- **Nemotron has an architecture-verification gate.** Before any `Nemotron3Adapter` code is written, confirm: (a) is the model hybrid Mamba-Transformer? (b) what is the `llm_arch` tag from `gguf-dump`? (c) is the chat template Llama-derived or Nemotron-specific? (d) what is the tool-call format? (e) does the model emit reasoning traces? If hybrid Mamba and llama.cpp state handling is unstable, the adapter is deferred to STALLED — no workaround.
- **Single-GPU constraint, one at a time.** Dev machine VRAM target. Quants pick is implementer's call; document each pick in a per-entry comment in `bundled_models.yaml`.
- **License verification per family.** Apache-2.0 / Gemma Terms of Use / NVIDIA Open Model License must each be checked against engine license (LGPL-3.0-or-later) + BISSELL linking exception. Document in registry comments.

## Scope

### Registry expansion (`data/bundled_models.yaml`)

Add 9 new entries:

```yaml
qwen3_5_0_8b:    { source: unsloth/Qwen3.5-0.8B,            adapter: qwen35,   ... }
qwen3_5_2b:      { source: unsloth/Qwen3.5-2B,              adapter: qwen35,   ... }
qwen3_5_4b:      { source: unsloth/Qwen3.5-4B-GGUF,         adapter: qwen35,   ... }
qwen3_5_9b:      { source: unsloth/Qwen3.5-9B,              adapter: qwen35,   ... }
qwen3_6_a3b:     { source: unsloth/Qwen3.6-35B-A3B-GGUF,    adapter: qwen36,   ... }
gemma4_a4b:      { source: unsloth/gemma-4-26B-A4B-it-GGUF, adapter: gemma4,   ... }
gemma4_e4b:      { source: unsloth/gemma-4-E4B-it-GGUF,     adapter: gemma4,   ... }
gemma4_e2b:      { source: unsloth/gemma-4-E2B-it-GGUF,     adapter: gemma4,   ... }
nemotron3_nano_4b: { source: unsloth/NVIDIA-Nemotron-3-Nano-4B-GGUF, adapter: nemotron3, ... }
```

Each entry includes the canonical fields (`path`, `context_length`, `recommended_quant`, license, etc.). Test fixture `tests/data/bundled_models.yaml` mirrors at least one entry per family.

### Adapter implementations

For each new adapter (`Qwen36Adapter`, `Gemma4Adapter`, `Nemotron3Adapter`):

1. New `{name}_adapter.{h,cpp}` under `src/inference/adapters/`.
2. Subclass `ChatAdapter` (or a shared `Qwen…Base` / `Gemma…Base` only if overlap is real).
3. Implement: `render_messages`, `parse_tool_calls`, `format_tool_result`, `format_tools`, `clean_content`, `format_system_with_vision`, `format_content_parts`.
4. Register in `src/inference/adapters/adapter_registry.cpp` with the appropriate key.
5. Doxygen on every public function (`@brief`, `@version 2.1.9`).

`Nemotron3Adapter` only after the architecture-verification gate documented in gh#47 passes.

### Tests

- **Unit tests** per adapter under `tests/unit/inference/{name}_adapter_test.cpp`:
  - Round-trip render: system + user + assistant + tool-result multi-turn
  - Tool-call parse: well-formed, malformed, partial / interrupted streams
  - Reasoning-trace handling (Nemotron, if applicable)
  - `format_content_parts` for multimodal user messages (Qwen36 — Gemma4 only if vision variant is bundled later)
- **Model tests** per adapter under `tests/model/`:
  - At least one bundled GGUF per family loads
  - First-token smoke
  - One tool-call fixture per family produces a parseable call

## Out of scope (deferred to other patches or follow-ups)

- Multimodal facade ABI work — landed in v2.1.8.
- Mid-generation user message queue — v2.1.10.
- Speculative decoding — v2.1.11.
- Vision-capable adapter variants (e.g., Qwen2.5-VL adapter if needed for v2.1.8's vision tier) — coordinate with v2.1.8 author; if v2.1.8 needs it, it lands there.
- Adapter coverage for Llama 3, Gemma 2, Gemma 3, Mistral / Mixtral, Phi-4, DeepSeek, Granite, Command-R — deferred to 2.3.x per gh#17 triage.
- `QwenBaseAdapter` / `GemmaBaseAdapter` refactor — only extract if overlap is real and worth the indirection.

## Validation criteria

1. 9 new registry entries land in `data/bundled_models.yaml` using the documented naming convention.
2. `tests/data/bundled_models.yaml` mirrors the schema with at least one entry per family.
3. Each fetch-tool key resolves and downloads cleanly.
4. Three new adapter classes are registered in `AdapterRegistry::create` with keys `qwen36`, `gemma4`, `nemotron3`.
5. Unit tests pass for each new adapter: render round-trip, tool-call parse (well-formed + malformed), multi-turn, reasoning-trace handling where applicable.
6. Model tests pass: each bundled GGUF (across all 3 Gemma 4 variants, Qwen3.6, Nemotron-3-Nano-4B) loads + first-token smoke + tool-call fixture.
7. Each bundled GGUF's license documented in `bundled_models.yaml` comments.
8. Nemotron architecture-verification gate documented in the `Nemotron3Adapter` file header (arch tag, template format, tool-call format, reasoning-trace handling, hybrid-Mamba status).
9. Doxygen on every new public symbol includes `@brief` and `@version 2.1.9`.
10. Pre-commit (knots, flake8, doxygen-guard, unit tests) clean.

## Pre-session reading (per project session protocol)

1. `docs/architecture-cpp.md` — full document
2. This proposal
3. All interface headers under `include/entropic/interfaces/`
4. `src/inference/adapters/qwen35_adapter.{h,cpp}` — structural template for all new adapters
5. `src/inference/adapters/adapter_base.{h,cpp}` — `ChatAdapter` base
6. `src/inference/adapters/adapter_registry.cpp` — registration pattern
7. Issue bodies for gh#44, gh#45, gh#46, gh#47, gh#17
8. Model cards for each new bundled model (consult unsloth repo READMEs + upstream model cards for template + tool-call format truth)
9. `extern/llama.cpp/src/llama-arch.cpp` — arch tag enumeration (for Nemotron gate)

## Implementation Log

### 2026-05-13 — Session 2 kickoff + Nemotron arch-verification gate

**Pre-session reads completed:**
- `docs/architecture-cpp.md` (focused: Class Hierarchy Patterns, Config
  Schema Pattern, Doxygen Standard)
- `include/entropic/inference/adapters/adapter_base.h` — actual
  `ChatAdapter` interface contract
- `src/inference/adapters/{adapter_base,adapter_registry,generic_adapter,
  qwen35_adapter}.{h,cpp}`
- `tests/unit/inference/qwen35_adapter_test.cpp` — test structure template
- `data/bundled_models.yaml` + `tests/data/bundled_models.yaml` — actual
  schema
- v2.1.8 STAGED implementation log (gh#42 redirect) — confirms no
  separate VL adapter; primary tier owns vision via existing
  `Qwen35Adapter` overrides
- `extern/llama.cpp/src/{llama-arch,llama-model}.cpp` and
  `src/models/nemotron-h.cpp` — Nemotron H support survey

**Nemotron arch-verification gate (gh#47) — PASSES.**
Findings:
- Architecture: hybrid Mamba-Transformer (Mamba-2 + MLP + 4 attention
  layers), compressed from Nemotron-Nano-9B-v2 via Nemotron Elastic
- GGUF arch tag: `nemotron_h` (also `nemotron_h_moe` for the MoE variant)
- llama.cpp status: **fully integrated.** `LLM_ARCH_NEMOTRON_H` is in
  the arch enumeration; `llm_build_nemotron_h` extends
  `llm_build_mamba_base` (stable Mamba state handling). Build path,
  KV cache sizing, and tensor loading all switch on the arch tag in
  `llama-model.cpp`. State handling is not experimental; it shares
  the Mamba base.
- Chat template: thinking-enabled by default, emits `<think>` and
  `</think>` as separate special tokens (use `--special` with
  llama.cpp CLI; programmatically handled by adapter's existing
  think-block parsing)
- Tool-call format: vLLM uses `qwen3_coder` parser — same XML format
  as Qwen 3.5 (`<tool_call><function=name><parameter=key>val
  </parameter></function></tool_call>`)
- Reasoning trace: yes — handled by `ChatAdapter::strip_think_blocks`
  and `extract_thinking` (already in base)

Decision: **proceed with `Nemotron3Adapter`.** Hybrid Mamba is not
unstable in this build — the gate's failure condition does not apply.

**Adapter contract correction (vs. proposal text):**
The proposal lists `render_messages` as a method to implement. The
actual `ChatAdapter` interface has no such method — chat-template
rendering is delegated to llama.cpp via the GGUF-embedded template.
Overridable surface:
- `parse_tool_calls(content)` — pure virtual
- `chat_format()` — pure virtual (returns format id or empty for
  GGUF-embedded)
- `format_tool_result(call, result)` — virtual
- `format_tools(tool_jsons)` — virtual
- `format_system_with_vision(base, has_vision)` — virtual (1.9.11)
- `format_content_parts(parts)` — virtual (1.9.11)

New adapters implement against this real surface. No `render_messages`
method is added.

**Registry schema correction (vs. proposal text):**
Proposal example fields (`source`, `path`, `context_length`,
`recommended_quant`, `license`) do not match the actual
`bundled_models.yaml` schema (`name`, `url`, `size_gb`, `adapter`,
`description`, optional `mmproj_key`). Proposal's intent — document
license + quant rationale — is fulfilled via per-entry YAML comments,
not new fields. Schema changes would be a design change requiring a
separate proposal; out of scope here.

**Open risk — Gemma 4 tool-call format.**
Upstream documentation surveyed so far describes Gemma 4 as having
"native function calling" but does not show raw output syntax. Output
uses a channel-based structure (`<|channel>thought\n...<channel|>`)
that is not the Qwen XML format. Decision deferred to when
`Gemma4Adapter` is being written; will pick from (a) defer to
STALLED, (b) use GGUF-embedded template via `chat_format=""` with a
permissive multi-format parser, or (c) constrain to JSON-tagged
fallback only and document the limitation in the adapter file
header. Recorded here so the choice is visible.

**Branch:** `feature/2.1.9-model-registry-and-adapters` (off
`develop`, proposal moved BACKLOG → ACTIVE in first commit).

### 2026-05-13 — Implementation complete, ready for STAGED

Four commits on the branch:

1. `chore(v2.1.9): move proposal to ACTIVE`
2. `feat(v2.1.9, gh#44): expand bundled_models.yaml with 9 model-keyed
   entries` — registry expansion + fixtures + Implementation Log seed
3. `feat(v2.1.9, gh#45/#46/#47): Qwen36, Gemma4, Nemotron3 adapters +
   unit tests` — three new chat adapter classes registered, 24+ new
   unit-test scenarios, registry factory refactored to a dispatch
   table to stay under the knots 3-return threshold
4. `test(v2.1.9): model tests for Qwen 3.6 / Gemma 4 / Nemotron 3
   families` — developer-run smoke + tool-call tests gated by
   `ENTROPIC_BUILD_MODEL_TESTS=ON`; SKIP cleanly when the bundled
   GGUF isn't on disk

**Validation criteria (proposal §Validation criteria):**

| # | Criterion | Status |
|---|---|---|
| 1 | 9 new registry entries land with `<family>_<size>` naming | ✅ |
| 2 | tests/data/bundled_models.yaml mirrors ≥1 entry per family | ✅ |
| 3 | Each fetch-tool key resolves cleanly | ⏳ developer-run via `entropic download <key>` — empirical at model-test phase per user redirect |
| 4 | Three adapters registered with keys qwen36 / gemma4 / nemotron3 | ✅ |
| 5 | Unit tests pass for each adapter (render round-trip, parse, multi-turn, reasoning) | ✅ 874 tests pass |
| 6 | Model tests pass: GGUF load + first-token smoke + tool-call fixture | ⏳ developer-run; test files compile + SKIP cleanly when GGUF absent |
| 7 | Each bundled GGUF's license documented in comments | ✅ Apache-2.0 / Gemma Terms / NVIDIA Open Model License — per entry |
| 8 | Nemotron arch-verification documented in adapter file header | ✅ see nemotron3_adapter.h §Architecture-verification gate |
| 9 | Doxygen on every new public symbol with @brief + @version 2.1.9 | ✅ doxygen-guard hook clean |
| 10 | Pre-commit clean (knots, flake8, doxygen-guard, unit tests) | ✅ all hooks green |

**Decisions made during implementation (not in original proposal):**

- **Gemma4Adapter format strategy: option (b) chosen.** Header
  documents the open question; implementation uses
  `chat_format=""` (GGUF-embedded template) + permissive tagged-JSON
  primary, bare-JSON fallback. Final refinement deferred to the
  model-test loop the user authorised.
- **Adapter registry factored to a dispatch table.** Going from two
  to five branches in `create_adapter()` pushed the function to six
  returns (knots threshold is three). Pulled the lookup into a
  `static const std::array<AdapterEntry, 4>` so the function stays
  at three returns and adding a sixth family is a one-row edit, not
  a refactor.
- **No `QwenBaseAdapter` extraction yet.** Qwen 3.5 vs Qwen 3.6 tool
  parsing genuinely overlaps today, but template/system-prompt
  divergence on the 3.x line is the eventual exit case for keeping
  them separate. Per proposal §Design decisions, extraction is a
  refactor for after a third Qwen generation lands or a real
  divergence demands it — not a precondition.
- **Registry schema unchanged.** Proposal example showed
  `source / path / context_length / recommended_quant / license`
  fields. The actual YAML schema uses `name / url / size_gb /
  adapter / description / mmproj_key`. Adding new fields would be a
  design change requiring a separate proposal; the proposal's
  intent (document licence + quant rationale) is fulfilled via
  per-entry comments.

Ready for STAGED.

### 2026-05-14 — Empirical validation closure

After STAGED move, the user authorised actual downloads + model tests
to close validation criteria #3 and #6 (which were still pending).
Six additional commits land the validation work:

5. `fix(v2.1.9, gh#44/#46): registry corrections from empirical
   download` — fixed mmproj filename collision (qwen3_6_a3b_mmproj
   was clobbering primary_mmproj because both had name `mmproj-F16`),
   404'd gemma4_a4b URL (no bare `Q4_K_M` in unsloth repo; switched
   to UD-IQ4_XS at 13.6 GB), Nemotron 3 quant pick (Q8_0 -> UD-Q4_K_XL).
   Added the two missing Gemma 4 variant model tests (E4B, A4B) so
   criterion #6 has coverage for all three Gemma 4 variants. Hardened
   `inv test` cpu lane with `-LE model` so stale model-test
   registrations can't make pre-commit hang.

6. `fix(v2.1.9): llama.cpp pin bump (gemma4 arch) + CUDA arch
   default + model-test cleanup` — final closure. Bumped
   `extern/llama.cpp` from `7f2cbd9a4` (Mar 19) to `253ba110b`
   (May 14, 732 commits) to add `LLM_ARCH_GEMMA4`. Fixed root
   `CMakeLists.txt` to default `CMAKE_CUDA_ARCHITECTURES` to `native`
   before `enable_language(CUDA)` so the dev preset picks the actual
   GPU arch (Blackwell sm_120 on this workstation) instead of CMake's
   default sm_52 — the bumped llama.cpp's optimized MoE-GEMV kernel
   (PR #20905) cannot run sm_52 binary on sm_120 hardware. Corrected
   v2.1.9 test assertions (REQUIRE_FALSE empty content was wrong for
   tool-only output where the adapter correctly strips the call —
   should assert on raw_content + tool_calls). Override
   `tier.context_length = 4096` in the v2.1.9 family helper to keep
   Gemma 4 A4B's larger KV cache (8 KV heads × 512 head_dim vs
   Qwen 3.6's 2 × 256) within the dev GPU's 16 GB VRAM budget.

**Final model-test outcome (`.venv/bin/inv test --model`):**
- 35/35 PASSED, 0 FAILED. Two flaky (gemma4_a4b retry 2, nemotron3
  retry 1) — cold-start memory contention that resolved on retry.
- All 30 pre-existing model tests still pass — no regression from
  the llama.cpp bump (gpu-profile CUDA error was a build-config bug,
  not an upstream regression).

**Empirical findings (model output observed in real inference):**
- **Qwen 3.6**: emits the qwen3_coder XML format — exactly the
  shape `Qwen36Adapter::parse_xml_function_calls` expects.
  `<tool_call><function=fs.read><parameter=path>/etc/hostname
  </parameter></function></tool_call>` was parsed cleanly.
- **Gemma 4**: emits **tagged JSON tool calls** —
  `<tool_call>{"name":"fs.read","arguments":{"path":"..."}}
  </tool_call>` — the permissive fallback in `Gemma4Adapter`
  (chosen as option (b) at design time) handles it via the base
  class's `parse_tagged_tool_calls`. The "open question" flagged
  in `gemma4_adapter.h` is resolved: tagged JSON. A future patch
  could tighten the adapter and remove the bare-JSON fallback,
  but the current permissive code is correct.
- **Nemotron 3**: emits `<think>...</think>` reasoning then the
  qwen3_coder XML call. The model may shorten tool names (e.g.
  `fs.read` -> `read`) as a prompt-following artifact; that's a
  prompting concern, not an adapter bug. Base-class think-block
  stripping works correctly.

**Validation criteria status (final):**
| # | Criterion | Status |
|---|---|---|
| 1 | 9 new registry entries with `<family>_<size>` naming | ✅ |
| 2 | tests/data/bundled_models.yaml mirrors per family | ✅ |
| 3 | Each fetch-tool key resolves and downloads cleanly | ✅ verified via `entropic download` for all 7 net-new GGUFs |
| 4 | Three adapters registered: qwen36 / gemma4 / nemotron3 | ✅ |
| 5 | Unit tests pass per adapter (874 scenarios total) | ✅ |
| 6 | Model tests: each bundled GGUF loads + smoke + tool-call | ✅ 35/35 |
| 7 | License documented per entry | ✅ |
| 8 | Nemotron arch gate documented in adapter header | ✅ |
| 9 | Doxygen @brief + @version 2.1.9 on every new symbol | ✅ doxygen-guard clean |
| 10 | Pre-commit (knots, flake8, doxygen-guard, unit tests) | ✅ |

Ready to merge to develop.
