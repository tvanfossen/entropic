# SPDX-License-Identifier: LGPL-3.0-or-later
#
# Python wrapper — v2.1.0 placeholder.
#
# The v1.7.x CMake-driven generate_wrapper.py pipeline has been removed.
# The v2.x wrapper (scripts/gen_python_bindings.py + python/src/entropic/)
# is a pure-Python wheel built and released independently of the C++ build.
# Generation runs at release time via `inv release-check`, not here.
#
# See .claude/proposals/ACTIVE/v2.1.0-formal-release.md (B2) for the
# full wrapper design and implementation plan.
#
# @brief Placeholder — Python wrapper is not built via CMake in v2.x.
# @version 2.1.0
